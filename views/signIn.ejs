<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sign In</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Custom CSS -->
  <style>
    body {
      background-color: #f0f0f0;
    }

    .signin-form {
      max-width: 400px;
      margin: 0 auto;
      padding: 20px;
      background-color: #fff;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    .signin-form h2 {
      margin-bottom: 20px;
      text-align: center;
      color: #333;
    }

    .signin-form label {
      font-weight: bold;
    }

    .signin-form .form-control {
      border-radius: 4px;
    }

    .signin-form .btn-primary {
      width: 100%;
      border-radius: 4px;
    }

    /* Additional Styles */
    .signin-form a {
      display: block;
      margin-top: 10px;
      text-align: center;
      color: #333;
    }

    .signin-form a:hover {
      text-decoration: none;
      color: #007bff; /* Blue color on hover */
    }

    /* Additional text and style */
    .signin-form .additional-text {
      text-align: center;
      margin-top: 30px;
      color: #555;
      font-size: 14px;
    }

    .signin-form .additional-text a {
      color: #007bff;
    }
  </style>
</head>

<body>

  <div class="container mt-5">
    <div class="signin-form">
      <h2>Sign In</h2>
      <form method="post">
        <div class="form-group">
          <label for="email">Email:</label>
          <input type="email" name="email" class="form-control" required>
        </div>
        <div class="form-group">
          <label for="password">Password:</label>
          <input type="password" name="password" class="form-control" required>
        </div>
        <button type="submit" class="btn btn-primary">Sign In</button>

        <!-- Link to New User Sign Up page -->
        <a href="/user/signUp">New User Sign Up</a>

      </form>

     
    </div>
  </div>

</body>

</html>
